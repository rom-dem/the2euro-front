interface Routes {
  login: string;
  home: string;
  the2euro: string;
  coins: string;
  myCoins: string;
  users: string;
}

const endpoints: Routes = {
  login: "/login",
  home: "/",
  the2euro: "/the2euro",
  coins: "/coins",
  myCoins: "/my-coins",
  users: "/users",
};

export default endpoints;
