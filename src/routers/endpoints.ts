interface Routes {
  login: string;
  home: string;
  the2euro: string;
  coins: string;
  myCoins: string;
}

const endpoints: Routes = {
  login: "/login",
  home: "/",
  the2euro: "/the2euro",
  coins: "/coins",
  myCoins: "/my-coins",
};

export default endpoints;
