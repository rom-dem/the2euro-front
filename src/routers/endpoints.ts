interface Routes {
  login: string;
  home: string;
  the2euro: string;
  coins: string;
}

const endpoints: Routes = {
  login: "/login",
  home: "/",
  the2euro: "/the2euro",
  coins: "/coins",
};

export default endpoints;
